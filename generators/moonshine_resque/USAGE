Description:
    Creates configuration files for deploying Resque

Example:
    ./script/generate moonshine_resque

    This will create:
      config/resque.yml
      config/initializers/load_resque.rb
